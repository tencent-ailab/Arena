from setuptools import setup

setup(
    name='Arena',
    version='1.3',
    description='Arena',
    keywords='Arena',
    packages=[
        'arena',
    ],

    install_requires=[
        'gym',
        'pillow',
        'pygame'
    ],
)
